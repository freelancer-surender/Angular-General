import { ColorBoxDirective } from './color-box.directive';

describe('ColorBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorBoxDirective();
    expect(directive).toBeTruthy();
  });
});
